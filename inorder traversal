def inorder(node):
  if node is None:
    return []
  return (inorder(node.left)+[node.key]+inorder(node.right))
  
# you need to initially declare tree node 
# for code simply go to the create_tree file in my datastructures repository
